*) Take the link from the input in shortener, probably
  put in a array of state in index and pass that to
  the container with all of the links, that is gonna
  have a max height and scroll-y
*) Finish the res

  The separation between your components and the edges of the
  screen is pretty much hard-coded into each component. Fix
  that by adding that same separation into wrapper, and that
  to all the elements of the page. Remember also that you 
  pretty much have to padding for the separation and not 
  margin